schema_version: 1
# This feedstock's own registry (conventions.md: releases carry it).
packages:
  - name: inkscape
    kind: toolkit
    summary: Vector graphics editor (CLI tools) — metanorma's graphics chain
    registry: tfs:github:tebako-packages/inkscape
